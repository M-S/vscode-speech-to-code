module.exports = {
  "add function": `function name(params) {
    
    }`,

  "add for each": `array.forEach(element => {
    
    });`,

  "add for in": `for (const key in object) {
        if (Object.hasOwnProperty.call(object, key)) {
          const element = object[key];
          
        }
      }`,
  "add if": `if (condition) {
          
      }`,
  "add if else": `if (condition) {
          
      } else {
          
      }`
};
